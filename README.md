make a virtual environment using python -m venv venv (if youre on windows)

then activate your virtual environment venv\scrips\activate (works for me man idk)

then do pip install -r requirements.txt the usual

